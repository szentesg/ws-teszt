<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Easy Readable Date Generator</title>
	</head>
	<body>
		<h1>Easy Readable Date Generator</h1>

		<form method="POST" action="{{ path('easy_readable_date') }}">
			<label for="seconds">Enter seconds:</label>
			<input type="number" id="seconds" name="seconds" min="1" required>
			<button type="submit">Generate Readable Date</button>
		</form>

		{% if formattedDate %}
			<h2>Formatted Date:</h2>
			<p>{{ formattedDate }}</p>
		{% endif %}
	</body>
</html>
